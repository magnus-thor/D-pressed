- @articles.each do |article|
  = link_to article.title, article_path(article)
  %p= article.title
  %p= article.body
  %p= article.created_at.to_date

.ad1
= link_to image_tag("test.png"), "https://craftacademy.se/"

  

= link_to 'Registration' , new_user_registration_path
